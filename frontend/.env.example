# Copy to .env.local and rebuild: npm run build

# One image for sidebar + login header (bird+name combined, or wordmark only)
# Optional: override bundled logo at /zydex-logo.svg
# VITE_BRAND_LOGO_URL=/zydex-logo.svg
